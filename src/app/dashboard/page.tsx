import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Dashabord | CoSplit"
};

const page = () => {
  return <div>Dashboard</div>;
};

export default page;
